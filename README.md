# Sticker Line Studio - Technical Documentation

โปรเจกต์นี้เป็น Mobile-First Web Application สำหรับสร้างสติ๊กเกอร์ LINE ระดับมืออาชีพ โดยใช้พลังของ **Gemini 3 Pro Image Preview** ในการสร้างภาพ และใช้ Client-side Image Processing ในการตัดแบ่งภาพและทำพื้นหลังโปร่งใส

---

## 🏗 โครงสร้างโปรเจกต์ (Project Structure)

- `App.tsx`: คอมโพเนนต์หลักที่จัดการ State, การอัปโหลดภาพ และการเชื่อมต่อ Workflow ทั้งหมด
- `services/geminiService.ts`: จัดการการส่ง Request ไปยัง Gemini API และการเขียน Prompt Engineering
- `utils/imageProcessor.ts`: อัลกอริทึมการประมวลผลภาพ (ตัดพื้นหลัง, แบ่งช่อง, Crop)
- `components/`: UI Components เช่น `StickerCard` สำหรับแสดงผลและดาวน์โหลด

---

## 🧠 คำอธิบายฟังก์ชันหลัก

### 1. การสร้างภาพ (Gemini Service)
**ไฟล์:** `services/geminiService.ts`

ฟังก์ชันหลักคือ `generateStickerSheet` ซึ่งรับภาพต้นฉบับและสไตล์ที่ต้องการ
- **Fixed 1K Resolution**: ระบบถูกล็อคความละเอียดไว้ที่ **1K** เนื่องจากเป็นความละเอียดที่สมดุลที่สุดสำหรับสติ๊กเกอร์ LINE (ซึ่งมีขนาดแสดงผลจริงเพียง 370x320 พิกเซล) การล็อคค่านี้ช่วยให้ลด Latency ในการ Generate ภาพลงได้มากกว่า 50% เมื่อเทียบกับความละเอียด 4K
- **Magenta Background Strategy**: เราสั่งให้ AI สร้างภาพบนพื้นหลังสี **Pure Magenta (#FF00FF)** เพื่อความแม่นยำสูงสุดในการทำ Chroma Key
- **LINE Compliance Prompting**: มีการฝังกฎ **LINE Creator Review Guidelines** ลงใน Prompt เพื่อคุมคุณภาพ:
    - **Consistency**: บังคับให้ตัวละครมีใบหน้าและชุดเดิมทั้ง 12 ท่า
    - **Safety Area**: เว้นระยะห่างรอบตัวสติ๊กเกอร์เพื่อป้องกันการตัดตก
    - **Die-cut Border**: บังคับให้มีเส้นขอบสีขาวหนา

### 2. การประมวลผลภาพ (Image Processor)
**ไฟล์:** `utils/imageProcessor.ts`

ขั้นตอนการประมวลผลภาพ Grid ออกเป็น 12 ไฟล์ PNG โปร่งใส:
1. **Grid Splitting**: แบ่ง 3x4 ช่องพร้อม Inset ป้องกันภาพข้างเคียง
2. **Flood Fill Alpha**: ลบพื้นหลัง Magenta ด้วยอัลกอริทึม Queue-based
3. **Fragment Removal**: ค้นหากลุ่มพิกเซลที่ใหญ่ที่สุดและลบเศษส่วนเกินทิ้ง (ป้องกันปัญหาภาพไม่ตรง Block)
4. **Tight Cropping**: ตัดขอบที่ว่างออกเพื่อให้สติ๊กเกอร์เต็มพื้นที่

---

## 🛠 การเชื่อมต่อกับ Python (Backend Integration)

หากคุณต้องการย้าย Logic บางส่วนไปไว้ที่ Python (เช่น ใช้ OpenCV หรือ PIL):
```python
# ตัวอย่างการทำ Chroma Key และลบ Fragment
import cv2
import numpy as np

def process_sticker(img_path):
    img = cv2.imread(img_path, cv2.IMREAD_UNCHANGED)
    # 1. Define Magenta Range & Create Mask
    # 2. Apply Connected Components
    # 3. Keep only the largest blob
    # 4. Crop to content
```
